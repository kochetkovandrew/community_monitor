<%= form_for(@copy_message) do |f| %>
  <% if @copy_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@copy_message.errors.count, "error") %> prohibited this copy_message from being saved:</h2>

      <ul>
      <% @copy_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_vk_id %><br>
    <%= f.number_field :user_vk_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :raw %><br>
    <%= f.text_area :raw %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
