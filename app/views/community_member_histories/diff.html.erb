<h4>Изменения в сообществе на <%= @community_member_history.created_at %></h4>

<p id="notice"><%= notice %></p>

<p>Добавлены:</p>

<table class="table table-striped table-condensed">
  <thead>
  <th>&nbsp;</th>
  </thead>
  <% @diff['added'].each do |vk_id| %>
  <tr>
    <td><%= link_to vk_id, 'https://vk.com/id' + vk_id.to_s %></td>
  </tr>
  <% end %>
</table>

<p>Удалены:</p>

<table class="table table-striped table-condensed">
  <thead>
  <th>&nbsp;</th>
  </thead>
  <% @diff['removed'].each do |vk_id| %>
      <tr>
        <td><%= link_to vk_id, 'https://vk.com/id' + vk_id.to_s %></td>
      </tr>
  <% end %>
</table>

<div style="clear: both;">
  <%= link_to 'Назад', @community_member_history.community %>
</div>
