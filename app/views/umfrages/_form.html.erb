<%= form_with(model: umfrage, local: true) do |form| %>
  <% if umfrage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(umfrage.errors.count, "error") %> prohibited this umfrage from being saved:</h2>

      <ul>
      <% umfrage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :result %>
    <%= form.text_area :result %>
  </div>

  <div class="field">
    <%= form.label :ip_address %>
    <%= form.text_area :ip_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
