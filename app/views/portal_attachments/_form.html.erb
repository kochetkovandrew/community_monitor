<%= form_with(model: portal_attachment, local: true) do |form| %>
  <% if portal_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portal_attachment.errors.count, "error") %> prohibited this portal_attachment from being saved:</h2>

      <ul>
      <% portal_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'Выберите файл', class: :file_label, for: :file_upload %>
    <%= form.file_field :file_upload, id: :file_upload, multiple: true %>
  </div>

  <div id="file_list">

  </div>

  <div class="actions">
    <%= form.submit 'Загрузить', class: 'btn btn-success' %>
  </div>
<% end %>
