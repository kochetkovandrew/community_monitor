<%= form_for(@community) do |f| %>
  <% if @community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community.errors.count, "error") %> prohibited this community from being saved:</h2>

      <ul>
      <% @community.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :screen_name %><br>
    <%= f.text_field :screen_name, disabled: !@community.new_record? %>
  </div>

  <div class="field">
    <%= f.label 'Отслеживать участников?' %><br>
    <%= f.check_box :monitor_members %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
